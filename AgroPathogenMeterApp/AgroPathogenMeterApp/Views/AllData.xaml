<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="AgroPathogenMeterApp.Views.AllData">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="{Binding Path=[IsInfected]}" Grid.Row="0" Grid.Column="0" />
            <Label Text="{Binding Path=[Name]}" Grid.Row="0" Grid.Column="1" />
            <Label Text="{Binding Path=[ID]}" Grid.Row="0" Grid.Column="2" />
            <Label Text="{Binding Path=[Date]}" Grid.Row="1" Grid.Column="0" />
            <Label Text="{Binding Path=[AmountBacteria]}" Grid.Row="1" Grid.Column="1" />
            <Label Text="{Binding Path=[ConcentrationBacteria]}" Grid.Row="1" Grid.Column="2" />
            <Label Text="{Binding Path=[VoltamType]}" Grid.Row="2" Grid.Column="1" />
            <Button Text="Previous Sample" Grid.Row="3" Grid.Column="0" Clicked="OnPreviousClicked"/>
            <Button Text="Next Sample" Grid.Row="3" Grid.Column="2" Clicked="OnNextClicked" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>